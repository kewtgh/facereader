# FaceReader 审计整改实施计划（2026-09-26）

对应审计：[project-audit-2026-09-26.md](project-audit-2026-09-26.md)。状态：代码整改完成，已通过本地验收。

## 顺序和验收

1. 阅读导航修复（A01–A04）：分类失败可重试/慢请求不覆盖新 hash；分类快速查找；多份双语 TOC 各自完整；文章语言准确；目录随语言重建且支持手机 active 状态。
2. 数据/评分契约（A05–A06）：共用浏览器和 CLI 校验，拒绝非法 schema/URL/分数/日期/模型；评分错误不降级成 0；单元测试覆盖缺值、越界、重复维度/名称。
3. 应用状态与页面说明（A07–A09）：请求超时/重试/一致错误状态，对比满位不替换，已提交查询不被草稿覆盖，修正 URL 拼接；实现页面内评分明细/阶段情景。按用户补充，不新增下载/导出功能，不把博客扩展为专业分析平台。
4. 回归（A10）：扩大单元与浏览器状态测试；单元测试纳入 site:check，浏览器测试单独执行；Jekyll build、site:check、Chromium 1243 桌面/移动 smoke；检查 Home/Posts/Categories/Tags/文章/About/LEADERS/benchmark。保存实际结果与仍需人工核查的范围。

## 约束

回归追加：A11 平板筛选栏溢出修复；A12 发布前强制 site:check；A13 搜索依赖失败的可见退路。均为现有博客功能可靠性改进。

- 不更改评分数据和业务阈值，不批量修改文章，无外部写入或发布。
- 不引入 heavy framework，不重做首页/品牌，不隐藏失败或用伪数据补位。
- 新固定文案有中英版本；键盘焦点和错误状态可访问。
- 不覆盖用户变更。整改完成后先保留可审阅 diff；用户随后要求更新版本并提交，本次发布版本为 7.1.9。

## 执行/验收记录

|阶段|结果|
|---|---|
|阅读导航 A01–A04|分类状态/重试/超时/旧请求保护、全部分类搜索和计数；多份 TOC 独立语言匹配，文章桌面/移动 active 同步；正文保留真实 lang。|
|数据与评分 A05–A06|新增 leaders-data.mjs 共用校验；纯函数拒绝重复维度和越界评级；未来复核日期/非法 URL/schema 可诊断；同名歧义不自动归属；防止继承属性被当成权重模式。|
|应用状态 A07–A09|加载/失败/重试状态一致；初次访问不默认展示任意企业；对比从空开始且满位不替换；草稿不会在语言/权重切换时成为已提交查询；锚点链接参数正确。评分明细含权重、贡献和阶段差异，默认折叠。|
|回归 A10–A13|原有单元检查扩展，新增可重跑 Chromium 脚本；修复 1024px 工具栏页面溢出；完整 site:check 已接到 Pages artifact 上传前；搜索依赖失败显示重试和文章归档退路。|

验证命令：`npm run leaders:test`、`npm run leaders:validate`、`npm run site:build`、`npm run site:check`、`npm run site:browser` 均执行通过。浏览器脚本需要已安装 Playwright，运行说明见 README；本机使用 Chromium 1243，测试启动的本地服务器与浏览器自动关闭。

- Jekyll 4.4.1 构建正常，HTML 检查 260 页/945 图片，0 图片尺寸问题、0 懒加载问题，内部链接通过。
- CSS：190885 bytes raw / 33570 gzip / 28163 brotli，预算通过；Sass 无新增弃用警告。
- 浏览器：1440/1024/390px 主页面及中英文长文无页面横向溢出；表格允许自身横向滚动。分类断网重试与慢响应过期、反复切换中英/双份目录、导航 Tags 保留、草稿隔离、满位对比、非法模型拦截后重试、异常 stage 降级、已有打印 clone 清理、搜索 SDK 被阻断时的错误退路通过，无未捕获脚本异常。
- 复现的 1024px 溢出已修复。手机 resize 时曾在下一帧布局前测到瞬时超宽，稳定后不复现；测试等待 responsive nav 的 rAF 后检查，未为此追加无依据的 Header 重写。
- 截图：`tmp/audit-2026-09-26/category-filter-1440.png`、`category-filter-390.png`、`score-explanation-1440.png`、`score-explanation-390.png`（本地忽略产物，不发布）。
- 保留资料治理提示：39 条缺复核日期/来源/共享别名提示，25 条模型差异复核提示。未伪造补齐、未重算或调整企业原始分数。

限制：本轮未执行真实外网 Algolia 检索/索引写入、未在 Safari/Firefox 或真实打印机上验收；没有修改已部署网站。网站仍是博客，不新增下载、账户或专业公司分析服务。发布 workflow 已修改，但只有推送并触发 CI 后才会在线执行；本次按用户后续要求更新为 7.1.9 并进行本地提交，不推送或部署。
